"""in vectorization we use numpy arrays to perform element-wise operations without the need for explicit loops.
"""
import numpy as np
arr=np.array([10,20,30])
arr_2=np.array([40,50,60])
result=arr+arr_2
print(result)

"""
can perform every operations like +, -, *, / """